# 占位档，仅为让 git 跟踪这个目录（git 不跟踪空目录）。
# 首次运行前本目录只有这个档；两个运行时状态档都是跑出来的，不随技能分发：
#   · last_run.json            —— 第 6 步 `snapshot.py write` 第一次成功执行后生成（之后每次运行覆写）
#   · dominance_history.jsonl  —— `crypto.sh dominance` 第一次成功取数后生成（一天一笔，同日重跑覆盖，最多 90 笔）
#                                 信号 16 的「7d 跌幅 >3%」靠它自答，要连跑 7 天才有答案；别 checkout 掉。
# 请勿删除本档。
